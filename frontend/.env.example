# Local dev — vite proxy handles /api → localhost:8000
# so no VITE_API_URL needed locally

# For production (Vercel), set this to your Render backend URL:
VITE_API_URL=https://your-leetcode-api.onrender.com/api/v1
